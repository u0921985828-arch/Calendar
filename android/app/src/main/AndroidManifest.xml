<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- El WebViewAssetLoader sirve la app bajo https://appassets.androidx.org/,
         y el WebView exige el permiso INTERNET para cargar cualquier esquema http/https
         AUNQUE el contenido sea local e interceptado. Sin él: net::ERR_CACHE_MISS.
         La app no hace tráfico de red real: sigue siendo 100% offline. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget de inicio: solo contadores del día y próximos. -->
        <receiver
            android:name=".NeuroWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/neuro_widget_info" />
        </receiver>
    </application>
</manifest>
